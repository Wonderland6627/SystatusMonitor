<Window x:Class="SystatusMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystatusMonitor"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300">
    <!--<Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="10">
            <TextBlock Text="{Binding CpuUsage}" FontSize="24"/>
            <TextBlock Text="{Binding MemoryUsage}" FontSize="24"/>
            <!--<Button Content="悬浮窗模式" Click="ShowFloatWindow_Click"/>-->
        </StackPanel>
    </Grid>
</Window>